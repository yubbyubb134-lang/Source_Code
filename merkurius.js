document.addEventListener('DOMContentLoaded', () => {
    // Anda bisa menambahkan fungsionalitas JavaScript di sini jika diperlukan.
    // Contoh: efek parallax ringan pada bintang saat scroll.

    // const stars = document.querySelector('.background-stars');
    // window.addEventListener('scroll', () => {
    //     const scrollY = window.scrollY;
    //     stars.style.transform = `translateY(${scrollY * 0.1}px)`;
    // });
});